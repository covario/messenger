import { Color, rgba } from '../../Utils/colors';

export const MuiDialogActions = {
  root: {
    backgroundColor: rgba(Color.Uranus),
    padding: 10,
    borderTop: `1px solid ${rgba(Color.Neptune)}`,
  },
};
